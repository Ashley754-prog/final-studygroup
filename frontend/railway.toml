[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run build && serve -s dist -l 3000"
healthcheckPath = "/"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
sleepForSeconds = 10

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
