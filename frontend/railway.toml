[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run build && serve -s dist"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
